type User {
  id: ID!
  email: EmailAddress!
  emailVerified: Boolean!
  profile: Profile
  createdAt: DateTime!
  updatedAt: DateTime
}

type Query {
  me: User!
}

type AuthPayload {
  accessToken: JWT
}

type Mutation {
  loginWithGoogle(idToken: NonEmptyString!): AuthPayload!
}
